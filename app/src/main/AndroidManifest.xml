<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/group_1__1_"
        android:label="@string/app_name"
        android:roundIcon="@drawable/group_1__1_"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChallengeMobileRecomendaes"
        tools:targetApi="s">

        <!-- Login Activity -->
        <activity android:name=".views.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Signup Activity -->
        <activity android:name=".views.SignupActivity" />

        <!-- Password Recovery Activity -->
        <activity android:name=".views.PasswordRecoveryActivity" />

        <!-- Recommendations Activity -->
        <activity android:name=".views.RecommendationsActivity" />

        <!-- User Profile Activity -->
        <activity android:name=".views.UserProfileActivity" />

        <!-- Search Activity -->
        <activity android:name=".views.SearchActivity" />

        <!-- Settings Activity -->
        <activity android:name=".views.SettingsActivity" />
    </application>

</manifest>
